{% set name = "paramak" %}

package:
  name: "{{ name|lower }}"
  version: {{ GIT_DESCRIBE_TAG }}

source:
  path: ..

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python {{ python }}
    - setuptools
  run:
    - cadquery
    # - gmsh [not win] TODO include in next release
    # - moab [not win] TODO include in next release
    - matplotlib
    - mpmath
    - numpy
    - plasmaboundaries
    - plotly
    - pytest-cov
    - python
    - scipy
    - sympy
    - nbformat
    - nbconvert
    - ipywidgets
    # - jupyter-cadquery not available on conda

test:
  imports:
    - paramak
    - paramak.parametric_components
    - paramak.parametric_reactors
    - paramak.parametric_shapes
    - tests
  requires:
    - pytest-cov
    - pytest-runner
  source_files:
    - tests/
    - examples/
  commands:
    - python tests/test_example_components.py
    - python tests/test_example_reactors.py
    - python tests/test_example_shapes.py
    - pytest -v tests


about:
  home: "https://github.com/fusion-energy/paramak"
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: "Create 3D fusion reactor CAD models based on input parameters"
  doc_url: https://paramak.readthedocs.io/
  dev_url: https://github.com/fusion-energy/paramak
  summary: Paramak - parameter driven fusion reactor model creation

extra:
  recipe-maintainers:
    - shimwell
