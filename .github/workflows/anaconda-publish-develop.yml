name: anaconda-publish-develop

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Set up conda
        run: |
            conda install -y anaconda-client conda-build
            conda config --set anaconda_upload yes
      - name: Build and publish to conda
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
        run: |
            conda build conda_dev -c fusion-energy -c conda-forge -c cadquery --config-file conda/conda_build_config.yaml

# this action does not currently convert to windows or OSX
