# Python CircleCI 2.0 configuration file
##
# Check https://circleci.com/docs/2.0/language-python/ for more details


version: 2
jobs:
  build:
    environment:
      IMAGE_NAME: openmcworkshop/paramak
    docker:
      - image: openmcworkshop/paramak:24220a4cef30d6e5119036d4ae39fd7d6034e0bf
    working_directory: ../
    steps:
      - run:
          name: run utils tests
          command: 

            pytest tests/test_utils.py -v --cov-report term --cov-report test-reports/coverage.xml --junitxml=test-reports/junit.xml
      - run:
          name: run shape tests
          command: 

            pytest tests/test_Shape.py -v --cov-report term --cov-report test-reports/coverage.xml --junitxml=test-reports/junit.xml

      # - run:
      #     name: run Reactor tests
      #     command: 

      #       pytest tests/test_Reactor.py -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # - run:
      #     name: run parametric_shapes tests
      #     command: 

      #       pytest tests/test_parametric_shapes/ -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # - run:
      #     name: run parametric_components tests
      #     command: 

      #       pytest tests/test_parametric_components/ -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # - run:
      #     name: run example shape tests
      #     command: 

      #       pytest tests/test_example_shapes.py -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # - run:
      #     name: run example component tests
      #     command: 

      #       pytest tests/test_example_components.py -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 
      # - run:
      #     name: run example reactor tests
      #     command: 

      #       pytest tests/test_example_reactors.py -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # - run:
      #     name: run parametric_reactors tests
      #     command: 

      #       pytest tests/test_parametric_reactors/ -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      # TODO once the open-source neutronics workflow is more stable include these tests
      # - run:
      #     name: run neutronics tests
      #     command: 

      #       pytest tests/test_parametric_neutronics/ -v --cov=paramak --cov-append --cov-report term --cov-report xml:cov.xml 

      - store_test_results:
          path: test-reports

      - store_artifacts:
          path: test-reports
      - run: apt-get -y install curl
      - run: curl -s "test-reports/" https://codecov.io/bash | bash
